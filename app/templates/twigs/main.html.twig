{% extends twigFolder ~ '/index.html.twig' %}

{% block content %}
    
    {# include all the templates configured in settings.html.yml #}

  {% include twigFolder ~ '/autoloaders.html.twig' %}

    {# or each loader individually

  {% include twigFolder ~ '/autoloader.html.twig' with {'section': SectionName} %}

    #}

{% endblock %}